# API server that /api/* is proxied to (used at build time by next.config.mjs)
API_ORIGIN=http://localhost:5000

# Socket.IO connects to the API server directly
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
